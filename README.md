# SnowWT
A simple, internals exposed implementation of Snowflake's authentication payload (password/jwt)

## Influence
The implementation references the snowflake-jwt crate and Snowflake clients (notably gosnowflake) to make a low overhead login auth payload for Snowflake.
